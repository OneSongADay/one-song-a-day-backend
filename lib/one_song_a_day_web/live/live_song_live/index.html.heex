<h1>Listing Live songs</h1>

<%= if @live_action in [:new, :edit] do %>
  <.modal return_to={Routes.live_song_index_path(@socket, :index)}>
    <.live_component
      module={OneSongADayWeb.LiveSongLive.FormComponent}
      id={@live_song.id || :new}
      title={@page_title}
      action={@live_action}
      live_song={@live_song}
      return_to={Routes.live_song_index_path(@socket, :index)}
    />
  </.modal>
<% end %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Youtube</th>
      <th>Spotify</th>

      <th></th>
    </tr>
  </thead>
  <tbody id="live_songs">
    <%= for live_song <- @live_songs do %>
      <tr id={"live_song-#{live_song.id}"}>
        <td><%= live_song.title %></td>
        <td><%= if live_song.youtube_link do %><div>youtube icon</div><% end %></td>
        <td><%= if live_song.spotify_link do %><div>spotify icon</div><% end %></td>

        <td>
          <span><%= live_redirect "Show", to: Routes.live_song_show_path(@socket, :show, live_song) %></span>
          <span><%= live_patch "Edit", to: Routes.live_song_index_path(@socket, :edit, live_song) %></span>
          <span><%= link "Delete", to: "#", phx_click: "delete", phx_value_id: live_song.id, data: [confirm: "Are you sure?"] %></span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<span><%= live_patch "New Live song", to: Routes.live_song_index_path(@socket, :new) %></span>
